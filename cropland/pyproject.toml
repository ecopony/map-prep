[project]
name = "cropland"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mapprep",
    "numpy",
    "pandas",
    "rasterio",
]

[tool.uv.sources]
mapprep = { workspace = true }
