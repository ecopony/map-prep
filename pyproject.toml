[tool.uv.workspace]
members = [
    "packages/mapprep",
    "projects/ambient-occlusion",
    "projects/carson-rem",
    "projects/conferences",
    "projects/cropland",
    "projects/forest-loss",
    "projects/hillshade",
    "projects/property-rainwater",
    "projects/rainfall",
]

# The pinned GDAL wheel is Windows/CPython 3.12 only, so lock for that platform.
[tool.uv]
environments = ["sys_platform == 'win32'"]
