[tool.uv.workspace]
members = [
    "mapprep",
    "ambient-occlusion",
    "carson-rem",
    "conferences",
    "cropland",
    "forest-loss",
    "hillshade",
    "property-rainwater",
    "rainfall",
]

# The pinned GDAL wheel is Windows/CPython 3.12 only, so lock for that platform.
[tool.uv]
environments = ["sys_platform == 'win32'"]
