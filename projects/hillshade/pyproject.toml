[project]
name = "hillshade"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mapprep",
    "matplotlib",
    "numpy",
    "pillow",
]

[tool.uv.sources]
mapprep = { workspace = true }
