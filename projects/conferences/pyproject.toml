[project]
name = "conferences"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "geopandas",
    "mapprep",
    "pandas",
    "plotly",
    "pyproj",
]

[tool.uv.sources]
mapprep = { workspace = true }
