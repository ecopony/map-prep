[project]
name = "property-rainwater"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mapprep",
    "python-dotenv",
    "rasterstats",
    "whitebox",
]

[tool.uv.sources]
mapprep = { workspace = true }
